# No root-level variables for the prototype. All configuration lives in
# local.alerts (locals_alerts.tf) and the module instantiation (main.tf).
#
# When extending to additional families later, pass family-specific
# overrides here.
